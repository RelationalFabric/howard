/**
 * Claims factory (plural)
 * 
 * The claims() function transforms predicates into claim objects.
 */

export { claims } from './factory.js'
export type { ClaimsInput, ClaimsResult, TransformPredicateName } from './types.js'

